spring:
  application:
    name: discovery-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      retry:
        max-attempts: 10
        max-interval: 1500
        initial-interval: 1000